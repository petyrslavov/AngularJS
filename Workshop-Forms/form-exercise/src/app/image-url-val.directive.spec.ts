import { ImageUrlValDirective } from './image-url-val.directive';

describe('ImageUrlValDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageUrlValDirective();
    expect(directive).toBeTruthy();
  });
});
